#include <stdio.h>
/**
 *main - This is the main entry
 *Return: Always return 0.
 */
int main(void)
{
	char letters;

	for (letters = 'a'; letters <= 'z'; letters++)
	{
		putchar(letters);

	}
	putchar('\n');

	return (0);
}
