#include "main.h"
/**
 *_isupper - This checks for uppercase characters
 *@c: character to be checked
 *Return: return 1 if is c is upper or othewise
 */
int _isupper(int c)
{
	int upper;

	for (upper = 65; upper <= 90; upper++)

	if (c >= 65 && c <= 90)
	return (1);
	return (0);

}
