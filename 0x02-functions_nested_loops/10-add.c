#include "main.h"
#include <stdio.h>

/**
 *add -  This adds the fuction and return result
 *Description: the addition and returning of result
 *Return: return addition of a and b
 */
int add(int a, int b )
{

	return (a + b);
}
